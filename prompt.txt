分成5步生成收支餘絀表：
1. 利用OCR在pdf當中找到收支餘絀表的頁面，擷取該頁面。
2. 將該頁面蓋到文字的紅色印章去除（如果蓋到數字，可以合理推回原來數值再用）。
3. 利用當年度欄位資料生成.csv收支餘絀決算表。
4. 將其轉換成易轉成sankey圖的形式，csv欄位為：
   來源,目標,金額,金額（萬元）,金額（萬元）（手動調整）,step from,step to
5. 只輸出csv內容（用```csv 包起來），不要多餘解釋文字。
